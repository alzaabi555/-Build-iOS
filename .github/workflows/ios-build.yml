name: Build iOS App

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install --legacy-peer-deps

      - name: Build Web Assets
        run: npm run build

      # 1. إنشاء مجلد الآيفون
      - name: Add and Sync iOS Platform
        run: |
          npx cap add ios
          npx cap sync ios

      # 2. نسخ ملف Google Service (من الجذر إلى داخل مجلد الآيفون)
      - name: Copy Google Service File
        run: |
          if [ -f "GoogleService-Info.plist" ]; then
            echo "✅ Found GoogleService-Info.plist in root, copying to iOS project..."
            cp GoogleService-Info.plist ios/App/App/
          else
            echo "⚠️ Warning: GoogleService-Info.plist NOT found in root! Google Login will fail."
          fi

      # 3. 👇 هذا هو السطر الحاسم: إضافة معرف العودة (URL Scheme)
      - name: Configure iOS URL Scheme for Google Auth
        run: |
          PLIST="ios/App/App/Info.plist"
          # هذا هو المعرف المعكوس من حسابك (مهم جداً)
          REVERSED_CLIENT_ID="com.googleusercontent.apps.87037584903-3uc4aeg3nc5lk3pu8crjbaad184bhjth"
          
          # التأكد من وجود المصفوفة وإضافتها
          /usr/libexec/PlistBuddy -c "Print :CFBundleURLTypes" $PLIST || /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes array" $PLIST
          /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes:0 dict" $PLIST
          /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes:0:CFBundleURLSchemes array" $PLIST
          /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes:0:CFBundleURLSchemes:0 string $REVERSED_CLIENT_ID" $PLIST
          
          echo "✅ Successfully injected URL Scheme: $REVERSED_CLIENT_ID"

      # 4. أذونات الكاميرا والصور
      - name: Inject iOS Permissions
        run: |
          PLIST="ios/App/App/Info.plist"
          /usr/libexec/PlistBuddy -c "Add :NSCameraUsageDescription string 'نحتاج الوصول للكاميرا لتحديث صورة الملف الشخصي'" $PLIST || /usr/libexec/PlistBuddy -c "Set :NSCameraUsageDescription 'نحتاج الوصول للكاميرا لتحديث صورة الملف الشخصي'" $PLIST
          /usr/libexec/PlistBuddy -c "Add :NSPhotoLibraryUsageDescription string 'نحتاج الوصول للصور لاختيار صورة الملف الشخصي'" $PLIST || /usr/libexec/PlistBuddy -c "Set :NSPhotoLibraryUsageDescription 'نحتاج الوصول للصور لاختيار صورة الملف الشخصي'" $PLIST
          /usr/libexec/PlistBuddy -c "Add :NSPhotoLibraryAddUsageDescription string 'نحتاج الصلاحية لحفظ التقارير كصور'" $PLIST || /usr/libexec/PlistBuddy -c "Set :NSPhotoLibraryAddUsageDescription 'نحتاج الصلاحية لحفظ التقارير كصور'" $PLIST

      - name: Generate App Icons
        run: npx capacitor-assets generate --ios

      - name: Build Xcode Archive
        run: |
          xcodebuild -workspace ios/App/App.xcworkspace \
                     -scheme App \
                     -configuration Release \
                     -destination 'generic/platform=iOS' \
                     -archivePath App.xcarchive \
                     archive \
                     CODE_SIGNING_ALLOWED=NO

      - name: Prepare App for Sideloading
        run: |
          mkdir -p build/Payload
          cp -R App.xcarchive/Products/Applications/App.app build/Payload/
          cd build && zip -r ../StudentManager.ipa Payload/

      - name: Upload IPA (Unsigned)
        uses: actions/upload-artifact@v4
        with:
          name: StudentManager-Unsigned-IPA
          path: StudentManager.ipa
