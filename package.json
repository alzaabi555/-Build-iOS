
{
  "name": "student-manager-app",
  "version": "1.0.0",
  "description": "نظام إدارة الطلاب - تطبيق آيفون",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step needed for static app'",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
