
{
  "name": "student-manager-app",
  "version": "1.0.0",
  "description": "نظام إدارة الطلاب - تطبيق آيفون",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html manifest.json www/ && npx esbuild index.tsx --bundle --outfile=www/index.js --format=esm --jsx=automatic --minify --external:react --external:react-dom --external:recharts --external:lucide-react",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "esbuild": "^0.25.0"
  }
}
