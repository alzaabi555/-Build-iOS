
{
  "name": "student-manager-app",
  "version": "1.0.0",
  "description": "نظام إدارة الطلاب - تطبيق آيفون",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && npx esbuild index.tsx --bundle --outfile=www/index.js --format=esm --jsx=automatic --minify && cp index.html manifest.json www/",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "lucide-react": "^0.454.0",
    "recharts": "^2.13.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "esbuild": "^0.25.0"
  }
}
